/**
 * Signal cron jobs — runs scheduled tasks for forex signals
 */
const startSignalCron = () => {
  console.log('⏰  Signal cron jobs initialised (placeholder)');
  // TODO: Add node-cron jobs here for automated signal generation
};

module.exports = { startSignalCron };
